Слой 0: Входные данные (80 значений)
        │
        ├───────────────────┐
        │                   ↓
Слой 1: 128 обычных нейронов + 1 bias-нейрон (значение=1.0)
        │ (каждый имеет связь с bias)
        ├─────────────┐
        │             ↓
Слой 2: 64 обычных нейрона + 1 bias-нейрон (значение=1.0)
        │
        ├────────┐
        │        ↓
Слой 3: 16 обычных нейронов + 1 bias-нейрон (значение=1.0)
        │
        ├─────┐
        │     ↓
Слой 4: 4 выходных нейрона

Входные нейроны:
input_vector = [
    # === БЛОК 1: ТЕХНИЧЕСКИЕ ИНДИКАТОРЫ (20 признаков) ===
    rsi(14),                    # 1. Текущее значение RSI
    rsi_slope(5),              # 2. Наклон RSI (изменение за 5 периодов)
    macd_line,                  # 3. Значение линии MACD
    macd_signal,               # 4. Значение сигнальной линии MACD
    macd_histogram,            # 5. Разница между ними
    bollinger_upper_distance,  # 6. Расстояние до верхней полосы Боллинджера (%)
    bollinger_lower_distance,  # 7. Расстояние до нижней полосы
    atr(14),                   # 8. Absolute True Range - абсолютная волатильность
    atr_ratio(14, 50),         # 9. ATR(14)/ATR(50) - изменение волатильности
    volume_ratio,              # 10. Объем / SMA(объем, 20)
    obv_slope,                 # 11. Наклон индикатора On-Balance Volume
    stoch_k,                   # 12. Stochastic %K
    stoch_d,                   # 13. Stochastic %D
    adx(14),                   # 14. Индекс направленного движения (сила тренда)
    di_plus,                   # 15. +DI (сила восходящего тренда)
    di_minus,                  # 16. -DI (сила нисходящего тренда)
    cci(20),                   # 17. Commodity Channel Index
    williams_r,                # 18. Williams %R
    mfi(14),                   # 19. Money Flow Index
    momentum(10),              # 20. Цена сейчас / цена 10 периодов назад

    # === БЛОК 2: ЦЕНОВАЯ СТРУКТУРА (15 признаков) ===
    body_to_range_ratio,       # 21. Тело свечи / весь диапазон (сила импульса)
    is_hammer,                 # 22. 1 если паттерн "молот", иначе 0
    is_shooting_star,          # 23. 1 если "падающая звезда"
    high_low_ratio(5),         # 24. (High за 5 периодов - текущая цена) / диапазон
    close_position_in_day,     # 25. (Close - Low дня) / (High дня - Low дня)
    volatility_ratio,          # 26. Дневной диапазон / недельный диапазон
    gap_from_previous_close,   # 27. Gap открытия от предыдущего закрытия (%)
    distance_to_vwap,          # 28. Отклонение от средневзвешенной цены
    support_distance,          # 29. Расстояние до ближайшего уровня поддержки (в ATR)
    resistance_distance,       # 30. Расстояние до сопротивления (в ATR)
    fractal_index,             # 31. Количество фракталов за последние N свечей
    renko_direction,           # 32. Направление последнего кирпича Ренко (1/-1)
    keltner_breakout,          # 33. Пробитие канала Кельтнера
    donchian_position,         # 34. Позиция в канале Дончиана
    parabolic_sar_direction,   # 35. Направление Parabolic SAR (1/-1)

    # === БЛОК 3: МУЛЬТИТАЙМФРЕЙМНЫЙ АНАЛИЗ (20 признаков) ===
    # Дневной vs часовой
    daily_trend_strength,      # 36. ADX на дневках
    hourly_trend_strength,     # 37. ADX на часах
    divergence_4h_daily,       # 38. 1 если дивергенция RSI 4H vs Daily
    weekly_ema_distance,       # 39. Расстояние до недельной EMA (%)

    # Мультипериодные скользящие
    ma_alignment,              # 40. Совмещение MA: (SMA10>SMA20>SMA50) = 1, обратно = -1
    ema_gradient_short,        # 41. Наклон EMA(20) в градусах
    ema_gradient_long,         # 42. Наклон EMA(50)

    # Сжатие волатильности
    bollinger_squeeze_daily,   # 43. Сжатие полос Боллинджера (дневки)
    bollinger_squeeze_hourly,  # 44. Сжатие (часы)

    # Синтезированные признаки
    timeframe_sync_index,      # 45. Индекс синхронности таймфреймов (0..1)
    trend_consistency,         # 46. Консистентность тренда на всех ТФ

    # Регрессионный анализ
    linear_regression_slope_daily,    # 47. Угол линейной регрессии за 30 дней
    quadratic_term_daily,             # 48. Квадратичный член (ускорение/замедление)
    seasonality_strength,             # 49. Сила сезонной компоненты
    volatility_clustering,            # 50. Кластеризация волатильности (GARCH-эффект)
    correlation_sp500,               # 51. Корреляция с S&P500 за 20 дней
    gold_ratio,                      # 52. Отношение цены к золоту (если торгуете валюты)
    vix_impact,                      # 53. Влияние индекса страха VIX
    yield_curve_slope,               # 54. Наклон кривой доходности (макроэффект)
    dollar_index_correlation,        # 55. Корреляция с индексом доллара

    # === БЛОК 4: ВРЕМЕННЫЕ И КОНТЕКСТУАЛЬНЫЕ (25 признаков) ===
    # Время
    sin_hour,                   # 56. sin(2π * час/24) - циклическое кодирование
    cos_hour,                  # 57. cos(2π * час/24)
    sin_day_of_week,           # 58. sin(2π * день_недели/7)
    cos_day_of_week,           # 59. cos(2π * день_недели/7)
    day_of_month_encoded,      # 60. День месяца нормализованный
    week_of_year_encoded,      # 61. Неделя года нормализованная
    is_monday_open,            # 62. 1 если это понедельник после выходных
    is_friday_close,           # 63. 1 если пятница перед выходными
    trading_session,           # 64. 0=Азия, 1=Европа, 2=Америка, 3=перекрытие

    # Рыночный контекст
    vix_level,                 # 65. Уровень индекса VIX (нормализованный)
    fear_greed_index,          # 66. Индекс страха и жадности
    market_regime,             # 67. 0=флэт, 1=тренд вверх, 2=тренд вниз, 3=высокая волатильность
    economic_calendar_impact,  # 68. Сила ближайшего экономического события (0..1)
    fed_meeting_proximity,     # 69. Дней до ближайшего заседания ФРС

    # Статистические
    sharpe_ratio_30d,          # 70. Шарп-рацио инструмента за 30 дней
    max_drawdown_30d,          # 71. Максимальная просадка за 30 дней
    volatility_regime,         # 72. Режим волатильности (низкая/средняя/высокая)
    autocorrelation_returns,   # 73. Автокорреляция доходностей 1-го порядка
    hurst_exponent,            # 74. Экспонента Херста (персистентность/антиперсистентность)

    # Психологические уровни
    round_number_distance,     # 75. Расстояние до ближайшего круглого уровня (1.1000 и т.д.)
    option_pain_price,         # 76. Цена максимальной боли по опционам (если доступно)
    gamma_exposure_level,      # 77. Уровень гамма-экспозиции

    # Риск
    value_at_risk_95,          # 78. VaR 95% за 1 день
    expected_shortfall,        # 79. Conditional VaR (более консервативная метрика)
    black_swan_probability,    # 80. Вероятность экстремального движения (по распределению)
]

Выходные нейроны:
	Выход			Активация	Диапазон	Смысл
1	Сигнал			tanh		-1...+1		Направление
2	% риска			sigmoid		0...2%		Риск от депозита
3	Прогноз роста	        softplus	0...∞%		На сколько % вырастет (если лонг)
4	Прогноз падения	        softplus	0...∞%		На сколько % упадет (если шорт)